# ──────────────────────────────────────────────────────────────────────────────
# .env.example — Variables d'environnement pour la production
#
# 1) Copier ce fichier : cp .env.example .env
# 2) Remplir les valeurs dans .env
# 3) Ne JAMAIS committer .env sur GitHub (il est dans .gitignore)
# ──────────────────────────────────────────────────────────────────────────────

# Identifiant GitHub du dépôt (format : owner/repo, ex: dodoschouv/ccd_toys-academy)
# Utilisé pour référencer les images Docker sur ghcr.io
GITHUB_REPOSITORY=dodoschouv/ccd_toys-academy

# Base de données
DB_USER=toys
DB_PASSWORD=CHANGER_CE_MOT_DE_PASSE
DB_NAME=toys_academy
DB_ROOT_PASSWORD=CHANGER_CE_MOT_DE_PASSE_ROOT

# Port d'exposition de l'application (à changer si 8080 est pris sur le serveur)
APP_PORT=8080

# Origine autorisée pour les appels API (URL publique de l'appli)
# Exemple : https://ton-domaine.fr ou http://IP_DU_SERVEUR:8080
CORS_ORIGIN=http://localhost:8080
