# ──────────────────────────────────────────────────────────────────────────────
# Pipeline CI/CD — Toys Academy
#
# CE QUE ÇA FAIT :
#   • Sur chaque push (toutes branches) ou pull_request :
#       → Construit les 3 images Docker pour vérifier que tout compile ✅
#   • Sur un push vers la branche "main" uniquement :
#       → Construit ET publie les images sur GitHub Container Registry (ghcr.io)
#         afin qu'elles soient disponibles pour le déploiement sur le serveur
#
# IMAGES PUBLIÉES (ghcr.io) :
#   ghcr.io/<owner>/ccd_toys-academy/optimisation:latest  (+ tag SHA du commit)
#   ghcr.io/<owner>/ccd_toys-academy/backend:latest       (+ tag SHA du commit)
#   ghcr.io/<owner>/ccd_toys-academy/frontend:latest      (+ tag SHA du commit)
#
# AUCUNE MODIFICATION du code des autres membres — ce fichier lit le code
# en lecture seule pour builder les images Docker existantes.
# ──────────────────────────────────────────────────────────────────────────────

name: CI/CD – Build & Publish Docker images

on:
  push:
    branches:
      - '**'          # Toutes les branches (main, develop, feature/*, etc.)
  pull_request:       # Toutes les Pull Requests

# Empêche deux runs en parallèle sur la même branche (évite les conflits)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  # github.repository → "dodoschouv/ccd_toys-academy"
  # On préfixe chaque image avec le registry et le repo
  IMAGE_BASE: ghcr.io/${{ github.repository }}

jobs:
  build-and-push:
    name: Build & Push (${{ matrix.service }})
    runs-on: ubuntu-latest

    # Permissions nécessaires :
    #   contents: read  → cloner le dépôt
    #   packages: write → pusher les images sur ghcr.io (uniquement sur main)
    permissions:
      contents: read
      packages: write

    # On lance le build pour les 3 services en parallèle grâce à la matrix
    strategy:
      fail-fast: false   # Si un service échoue, continue de builder les autres
      matrix:
        include:
          - service: optimisation
            context: ./optimisation
            dockerfile: ./optimisation/Dockerfile

          - service: backend
            context: ./web/backend
            dockerfile: ./web/backend/Dockerfile

          - service: frontend
            context: ./web/frontend
            dockerfile: ./web/frontend/Dockerfile

    steps:
      # 1) Récupérer le code du dépôt
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      # 2) Activer Docker BuildKit (builds multi-stage plus rapides + cache)
      - name: 🐳 Configurer Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3) Se connecter à ghcr.io — UNIQUEMENT si on est sur main
      #    GITHUB_TOKEN est automatique, aucun secret à configurer dans le repo
      - name: 🔐 Login sur GitHub Container Registry
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 4) Builder (+ pusher si on est sur main) l'image du service courant
      #
      #    Tags générés :
      #      :latest     → toujours la dernière version de main
      #      :sha-XXXXXX → version immutable liée à un commit précis
      #
      #    cache-from/cache-to → GitHub Actions cache : accélère les rebuilds
      #    (Maven ne re-télécharge pas les dépendances, npm non plus)
      - name: 🔨 Build${{ github.ref == 'refs/heads/main' && github.event_name == 'push' && ' & Push' || '' }} — ${{ matrix.service }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
          tags: |
            ${{ env.IMAGE_BASE }}/${{ matrix.service }}:latest
            ${{ env.IMAGE_BASE }}/${{ matrix.service }}:sha-${{ github.sha }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,scope=${{ matrix.service }},mode=max
          # Métadonnées pour tracer l'image jusqu'au commit source
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ github.event.head_commit.timestamp }}
